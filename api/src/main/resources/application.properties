spring.application.name=study

# 数据源配置（生产环境建议使用环境变量注入敏感信息）
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/school?allowPublicKeyRetrieval=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC&rewriteBatchedStatements=true
spring.datasource.username=root
# 密码建议通过环境变量配置: spring.datasource.password=${MYSQL_ROOT_PASSWORD:}
spring.datasource.password=Chaucey9990828

# MyBatis 配置
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.configuration.map-underscore-to-camel-case=true

server.port=8081
